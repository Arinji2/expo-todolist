import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Index() {
	return <SafeAreaView className="w-full h-full bg-neutral-700"></SafeAreaView>;
}
